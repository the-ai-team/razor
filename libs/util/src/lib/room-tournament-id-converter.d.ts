import { RoomId, TournamentId } from '@razor/models';
export declare const tournamentIdToRoomId: (tournamentId: TournamentId) => RoomId;
export declare const roomIdToTournamentId: (roomId: RoomId) => TournamentId;
